/**
 * angular2-google-map - Angular2 components for Google Maps
 * @author Stanislav Kiryukhin <korsar.zn@gmail.com>
 * @version v0.1.7
 * @link 
 * @license MIT
 */
import { OpaqueToken } from 'angular2/core';
import { LoaderGoogleMapsApi } from '../loader';
export declare enum ScriptLoaderProtocol {
    AUTO = 0,
    HTTP = 1,
    HTTPS = 2,
}
export interface LoaderOptions {
    apiKey?: string;
    apiVersion?: string | number;
    protocol?: ScriptLoaderProtocol;
    hostAndPath?: string;
    libraries?: string[];
    region?: string;
    language?: string;
}
export declare const LAZY_LOADER_OPTIONS: OpaqueToken;
export declare const LOADER_OPTIONS_DEFAULT: LoaderOptions;
export declare class LazyLoaderGoogleMapsApi extends LoaderGoogleMapsApi {
    private _options;
    private _promise;
    constructor(options: LoaderOptions);
    load(): Promise<void>;
    private createPromise();
    private createScript(callbackName, onError);
    private createScriptUrl(callbackName);
    private getProtocol();
    private getQueryParams(callbackName);
}
